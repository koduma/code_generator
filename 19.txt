//shout

#include<stdio.h>
int main(){
  int n,t[1000],shout_t;
  scanf("%d",&n);
  for(int i=0;i<n;i++) scanf("%d",&t[i]);
  shout_t=t[0];
  for(int i=1;i<n;i++) if(shout_t>t[i]) shout_t=t[i];
  printf("%d\n",shout_t);
  return 0;
}